<?php

namespace {{namespaceOverrides}};

use Alexpr94\BitrixModels\Models\IBlock\Section\IBSectionPropsData;
use Alexpr94\BitrixModels\Models\IBlock\TypeValuesFields\BaseValueField;

class {{nameClass}}SectionPropsData extends IBSectionPropsData
{
{{props}}
    protected function mapProp2Field(): array
    {
        return [
{{map}}
        ];
    }

    public static function labels(): array
    {
        return [
{{labels}}
        ];
    }
}